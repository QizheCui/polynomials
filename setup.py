from setuptools import setup, find_packages

setup(
    name="polynomials",
    version="0.1",
    packages=find_packages()
)
# to install, type in command line
# python3 _m pip install -e polynomials
